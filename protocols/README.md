# Sample protocols

Drop protocol PDFs here (e.g. `protocol1.pdf`, `protocol5.pdf`,
`protocol9.pdf`, `protocol12.pdf`, `protocol15.pdf`).

PDFs in this folder are gitignored — the documents themselves are not
committed; only their extraction outputs (in `../outputs/`) are.
