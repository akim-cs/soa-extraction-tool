# UI (Phase 8)

Static page served by the FastAPI backend: uploads a protocol PDF, runs the
pipeline, renders the extracted SoA as an HTML table (hierarchical headers,
verbatim cells, footnote markers as superscripts with tooltips).
